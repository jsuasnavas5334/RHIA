# Registro de tarea PH03-T002

```text
TASK: PH03-T002
STATUS: DONE
COMMIT: No creado; publicación bajo control humano.
```

## Cambios

- Creado `@rhia/policy` con roles humanos, identidades de servicio, permisos y capabilities.
- Definidos ceilings inmutables para Agent, n8n y Worker Service.
- Modeladas acciones sensibles y proof de approval humano separado.
- Documentada la matriz en `docs/security/rbac-capability-policy.md`.

## Pruebas y resultados

- Typecheck dirigido: aprobado.
- Permission matrix y negative authorization: 7/7 aprobadas.
- Operador sin acceso a price books, viewer sin jobs y agent sin autoelevación: aprobados.
- Autoaprobación, approval expirado/incorrecto y capability inyectada: rechazados.

## Riesgos y pendientes

- Core/worker deberán cargar approval y principal desde persistencia auditada.
- Las migrations todavía no se aplican a la base activa.

## Persistencia y gate

- Seed idempotente: 17 permisos, 4 roles, 40 role grants, 8 capabilities y 18 service grants.
- Identidades `AGENT_SERVICE`, `N8N_SERVICE` y `WORKER_SERVICE` permanecen inactivas.
- PostgreSQL rechazó user-role y approval cross-tenant.
- PostgreSQL rechazó `approved-actions.execute` para `commercial-agent`.
- Tres migrations, seeds dobles, conteos legacy y segundo restore limpio: aprobados.

## Siguiente tarea

Ejecutar `PH03-T003`: policy de outreach versionada con quiet hours, stop/suppression y simulación de 100 secuencias.
