# Registro de tarea PH02-T002

```text
TASK: PH02-T002
STATUS: DONE
COMMIT: No creado; publicación bajo control humano.
MIGRATIONS: Solo dry-run en PostgreSQL 18 temporal.
```

## Cambios

- Congelado el stack de RHIA v1 en `docs/architecture/stack.md`.
- Aceptado `ADR-0001` con Node.js 24.19 LTS, TypeScript strict, npm workspaces, PostgreSQL 18, Drizzle y Zod.
- Agregados configuración raíz del monorepo y spike comparable Prisma/Drizzle.
- Conservado el SQL generado de ambos candidatos; Prisma fue retirado de las dependencias instaladas.

## Pruebas

- `npm run typecheck`: aprobado.
- `npm run build`: aprobado.
- `scripts/test-orm-spike.sh`: aprobado dos veces.
- `npm audit --omit=dev`: 0 vulnerabilidades.
- Auditoría completa: 4 moderadas transitivas y exclusivas de tooling de desarrollo.

## Evidencia

- Drizzle: 1 tabla, 4 columnas, 3 índices.
- Prisma: 1 tabla, 4 columnas, 3 índices.
- Ningún SQL generado contiene operaciones destructivas.
- La segunda prueba aplicó los SQL guardados sin regenerar la migración Prisma.
- Cero contenedores o redes temporales `rhia-orm-spike` restantes.

## Riesgos

- `drizzle-kit` conserva una cadena transitiva de `esbuild` con cuatro alertas moderadas en desarrollo.
- La corrección sugerida por npm exige una versión breaking y no fue aplicada automáticamente.
- Next.js, ESLint y Playwright se incorporarán cuando existan sus packages funcionales; no se instalaron prematuramente.

## Decisiones

- Drizzle queda congelado para PH03-PH12 salvo nuevo ADR.
- Las migraciones se generan como SQL revisable y se prueban primero en PostgreSQL temporal.
- No introducir Redis, Kafka, Kubernetes ni microservicios en v1.

## Siguiente tarea

Completar `PH02-T001`: adopción y verificación reproducible del repositorio fuente sin commits ni push automáticos.
