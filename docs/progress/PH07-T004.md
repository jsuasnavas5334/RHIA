# PH07-T004 -- Implementar Opportunity Scoring

**Estado:** DONE (alcance "Scoring v1" del packet -- motor puro de scoring, sin wiring a `OpportunityService.create` real; ver "Fuera de alcance de este ciclo").
**Sesion:** SES-20260906-71 (Cowork, ciclo programado, carpeta `Software RHIA` conectada via `device_bash`, misma conversacion que `PH07-T002`/`PH07-T003`).
**Fecha:** 2026-09-06 America/Guayaquil (2026-09-07 madrugada UTC).

## Contexto leido antes de escribir codigo

- `docs/progress/PH07-T003.md` (recien cerrado en esta misma sesion): dependencia satisfecha.
- Task Packet completo de `PH07-T004` en `PLAN_MAESTRO.md` (seccion 24, linea ~2730).
- `packages/db/src/schema.ts`: `opportunity.score` (`numeric(6,3)`) y `opportunity.score_version` (`text`) YA EXISTEN (PH03-T001), igual que `opportunity_signal` (`signal_type`, `signal_value` jsonb, `evidence_id` **NOT NULL**, `weight`). `apps/core-api/src/contracts.ts` (`OpportunitySchema`) los trata todavia como literales fijos `z.literal(0)`/`z.literal('core-v1')` -- confirmado con grep, sin ningun caller que produzca `matchedCriteria`/`signals`/`sendableContactPoints`/`evidenceCount` reales todavia (esas senales no existen como payload en `CreateOpportunitySchema`). Se documenta como gap real -- ver "Decision de alcance".

## Decision de alcance

Wirear el motor directo a `OpportunityService.create` habria significado, con el payload actual de `CreateOpportunitySchema` (solo `companyGroupId`/`primaryEntityId`/`marketCountry`/`marketCity`/`ownerUserId`/`nextActionAt`), **inventar** valores de `matchedCriteria`/`signals`/`sendableContactPoints`/`evidenceCount` -- exactamente el error a evitar del propio motor ("no se infla el score sin soporte real", ver `components.ts`). En vez de eso, y siguiendo el mismo patron de ubicacion ya usado en `PH05-T002`/`PH06-T002`/`T003`/`T004`/`PH07-T002` (el packet solo senala "scoring" como area afectada, sin caller real todavia), este ciclo construye `packages/opportunity-scoring` (`@rhia/opportunity-scoring`) como motor puro: recibe senales YA calculadas por el llamador (mismo principio que `@rhia/entity-resolver`/`@rhia/evidence-pipeline`) y devuelve un score + breakdown listo para escribir en `opportunity.score`/`opportunity.score_version` cuando exista un caller real con esas senales (p. ej. cuando `@rhia/contact-discovery` y Contact Validation, `PH07-T002`/`T003`, se conecten a un flujo real de creacion de oportunidades).

## Que se construyo

Paquete nuevo `packages/opportunity-scoring` (`@rhia/opportunity-scoring`), hermano de `packages/entity-resolver`/`packages/contact-discovery`.

- `schema.ts`: `ScoreComponentIdSchema` (`FIT | SIGNAL | CONTACTABILITY | TIMING | GEO_PRIORITY | EVIDENCE` -- accion 1, "Definir componentes"), `ScoringWeightsSchema` (accion 2, "Configurar pesos" -- todos configurables, nunca hardcodeados en el motor), `ScoreBreakdownEntrySchema` (accion 6, "Explicar score" -- cada componente trae `rationale`), `OpportunityScoreResultSchema` (accion 5, "Versionar score" -- `scoreVersion` viaja siempre junto al score).
- `geo-priority.ts` (accion 3, "Agregar country/city priority" + error a evitar "Hardcode if/else por pais"): `DEFAULT_COUNTRY_PRIORITY_TABLE` (`EC: 1, PE: 0.85`) es una TABLA de datos, no una cadena de `if`; `DEFAULT_COUNTRY_PRIORITY = 0.5` (nunca 0) es el piso para cualquier pais no listado -- criterio de aceptacion "Otros paises siguen elegibles". `countryPriority`/`geoPriority` aceptan una tabla propia del llamador (criterio "Ecuador y Peru reciben prioridad relativa configurable").
- `components.ts`: `fitScore`, `signalScore` (una senal sin evidencia real -- imposible en el esquema real, `opportunity_signal.evidence_id` es NOT NULL -- nunca cuenta), `contactabilityScore`, `timingScore` (decae linealmente sobre una ventana configurable, vencida = 0), `evidenceScore` (0 evidencia = 0 EXACTO, nunca un valor positivo "por si acaso" -- Pruebas requeridas "No evidence test").
- `scoring.ts`: `computeOpportunityScore` -- combina los 6 componentes con los pesos (normalizados por su suma), arma el breakdown explicable, nunca bloquea un mercado (accion 4 -- `geoPriority` nunca da 0, asi que ningun pais queda excluido del score total).
- `index.ts`: barrel export.

## Evidencia real (no simulada)

- `tsc --noEmit` real (`/usr/local/lib/node_modules_global/bin/tsc`, nativo `7.0.2`) sobre `packages/opportunity-scoring/tsconfig.json`: **exit 0** a la primera (sin errores de `exactOptionalPropertyTypes`/`noUncheckedIndexedAccess` que corregir -- diseño mas simple que `contact-discovery`, sin dependencias externas mas alla de `@rhia/contracts`).
- Build real -> `dist/*.js`: **exit 0**.
- `node --test packages/opportunity-scoring/dist/*.test.js`: **32/32 pass**, 0 fail, 0 skip a la primera corrida (sin necesidad de segunda ronda de correccion, distinto a `PH07-T002`/`T003` de esta misma sesion). Cobertura:
  - `geo-priority.test.ts` (9): Ecuador prioridad 1, Peru > pais no listado y < Ecuador, pais no listado NUNCA 0 (criterio "otros paises siguen elegibles"), insensible a mayusculas, tabla configurable, ciudad con/sin match.
  - `components.test.ts` (13) -- **incluye "No evidence test"**: `signalScore([])` = 0, `evidenceScore(0)` = 0 exacto; `fitScore` neutro sin criterios; `timingScore` vencida = 0, hoy = 1, decae linealmente; `contactabilityScore` proporcion real.
  - `scoring.test.ts` (10, mas 1 de "no bloquear mercados" y 1 de validacion final = 12 en total en el archivo) -- **incluye "Score versioning" y "Cross-country test"**:
    - Score versioning: version por defecto (`scoring-v1`), version explicita distinta se respeta, cambiar PESOS (no version) mantiene la version -- son configuracion, no un algoritmo nuevo.
    - Cross-country: Ecuador > Peru > pais no listado en `GEO_PRIORITY`, con el resto de senales identicas; tabla de prioridad geografica configurable sin tocar el motor.
    - No evidence test (a nivel de motor completo): sin senales ni evidencia, `SIGNAL`/`EVIDENCE` quedan en 0 explicito con `rationale` que lo documenta, el score total sigue siendo un numero valido (no `NaN`, no negativo).
    - No bloquear mercados: un pais no listado (`ZZ`), incluso con TODAS las demas senales en cero, produce un score `> 0` (FIT neutro + GEO_PRIORITY con piso >0 lo sostienen).
  - **Validacion final del packet** ("100 oportunidades pueden ordenarse y explicar top 10"): 100 oportunidades sinteticas (distintos paises/senales/timing) se calculan, se ordenan por `score` descendente, y las top 10 traen `breakdown` completo (6 componentes, cada uno con `rationale` no vacio) -- test explicito, no una afirmacion sin verificar.
- `tsc --noEmit` de `apps/core-api` (regresion, no se toco): **exit 0**.
- `grep -rl " $"` / `grep -rlP '\t'` sobre `packages/opportunity-scoring/src/*.ts`: sin resultados. Revision de secretos: sin resultados.
- Symlink de workspace: `node_modules/@rhia/opportunity-scoring -> ../../packages/opportunity-scoring` (gitignored).

## Criterios de aceptacion del packet

- [x] Ecuador y Peru reciben prioridad relativa configurable -- `DEFAULT_COUNTRY_PRIORITY_TABLE` + parametro `countryPriorityTable` en `computeOpportunityScore`, verificado con test.
- [x] Otros paises siguen elegibles -- `DEFAULT_COUNTRY_PRIORITY = 0.5` (nunca 0), verificado con test y con el test de "no bloquear mercados".
- [x] Score explicable -- cada `ScoreBreakdownEntry` trae `rawScore`/`weight`/`contribution`/`rationale`, verificado con test.

## Pruebas requeridas del packet

- [x] Cross-country test.
- [x] No evidence test.
- [x] Score versioning.

## Errores que debia evitar (verificados)

- [x] Hardcode if/else por pais -- `geo-priority.ts` usa exclusivamente tablas de datos (`Record<string, number>`), nunca una rama condicional por codigo de pais.
- [x] Score opaco de IA -- el motor es 100% determinista (funciones puras, sin ningun modelo de lenguaje ni llamada a `@rhia/ai-gateway`), cada componente y su combinacion son auditables linea por linea, con `rationale` explicito.

## Fuera de alcance de este ciclo (no bloquean el cierre `DONE`, documentado explicitamente)

1. **Wiring a `OpportunityService.create`**: `OpportunitySchema.score`/`scoreVersion` siguen siendo literales fijos `0`/`'core-v1'` en `apps/core-api/src/contracts.ts` -- conectar el motor real requiere que `CreateOpportunitySchema` (o un endpoint de recalculo dedicado) reciba senales reales de fit/signal/contactabilidad/evidencia, que hoy no existen como payload. Inventar esas senales para forzar el wiring habria violado el propio principio del motor ("no se infla el score sin soporte real") -- se documenta como gap real, mismo patron que `PH06-T003`/`T004` dejaron su propia integracion de persistencia para un ciclo posterior.
2. **`opportunity_signal` real**: el componente `SIGNAL` esta disenado para consumir filas reales de esa tabla (`evidence_id` NOT NULL garantiza que toda senal real tiene evidencia) pero ningun codigo la lee/escribe todavia (mismo estado que `product`/`price_book` que `PH07-T002` documento, y que `contact_point` antes de `PH07-T003`).
3. **Contactabilidad real desde `PH07-T003`**: `contactabilityScore` recibe `sendablePoints`/`totalPoints` ya contados por el llamador -- no importa `@rhia/contact-discovery` ni `isSendableContactPoint` de `apps/core-api` directamente (evita acoplar un paquete de `packages/*` con `apps/core-api`), pero el CONCEPTO es identico al de `PH07-T003` -- una integracion futura los conectaria con una funcion `.filter(isSendableContactPoint).length`.
4. **Ciudades priorizadas mas alla de la tabla vacia por defecto**: `cityPriority` esta implementado y probado, pero `DEFAULT` no trae ninguna ciudad precargada (ninguna fuente real de que ciudades priorizar dentro de EC/PE existe todavia) -- queda vacio a proposito en vez de inventar datos.
5. `scripts/verify-repository-baseline.ps1` (PowerShell): no se pudo ejecutar desde este sandbox Linux -- mismo limite de siempre.

## Siguiente tarea

`PH07-T004` esta `DONE`. Sigue `PH07-T005` -- Crear Next Best Action (dependencias `PH07-T004` DONE, `PH05-T003` DONE; rol recomendado `AI`; "puede ejecutarse en paralelo con": N/A). Leer su Task Packet completo en `PLAN_MAESTRO.md` (seccion 24, linea ~2784) antes de empezar.
