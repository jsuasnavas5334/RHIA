# Registro de tarea PH01-T002

```text
TASK: PH01-T002
STATUS: DONE
COMMIT: No creado; publicación bajo control humano.
MIGRATIONS: Ninguna.
```

## Cambios

- Creado `docs/baseline/database.md`.
- Exportados schemas sin datos de `rhia_core` y `n8n`.
- Clasificadas las 8 tablas RHIA como conservar/modificar o conservar/migrar.
- Preparados scripts de backup AES-256 y restore aislado con verificación de conteos.
- Ampliada la verificación de conteos desde una lista parcial a todas las tablas de aplicación de ambas bases.
- Añadida validación de integridad estructural y cobertura completa del manifest `counts.tsv`.
- El bundle se publica ahora mediante un renombrado atómico después de completar todos sus artefactos.
- Creada una passphrase aleatoria fuera del repositorio con ACL privada de Windows.
- Creado el bundle cifrado completo de `rhia_core` y `n8n`.
- Definida retención inicial de 7 bundles válidos, sin borrado automático en PH01.

## Pruebas

- Consultas de catálogo y conteos exactos completadas.
- Importación limpia de `rhia_core.schema.sql`: 8 tablas y 1 función.
- Importación limpia de `n8n.schema.sql`: 125 tablas.
- Contenedor PostgreSQL temporal eliminado después de validar.
- Servicios activos no reiniciados ni modificados.
- Sintaxis de ambos scripts validada con `bash -n`.
- Guardas de destino y passphrase probadas; no dejaron archivos residuales.
- La enumeración de catálogo confirmó 8 tablas de `rhia_core` y 125 de `n8n`, todas con identificadores compatibles con la verificación segura.
- El verificador ahora falla ante manifests vacíos, duplicados o incompletos antes de declarar válido el restore.
- Las ejecuciones fallidas no dejan un directorio con apariencia de bundle final válido.
- Los 5 artefactos del bundle pasaron SHA-256.
- El restore aislado reconstruyó 8 tablas de `rhia_core` y 125 de `n8n`.
- Los conteos de las 133 tablas restauradas coinciden exactamente con el origen.
- El contenedor temporal fue eliminado y los 4 servicios activos siguieron en ejecución.

## Evidencia

- Baseline: `docs/baseline/database.md`.
- DDL: `docs/baseline/database/rhia_core.schema.sql`.
- DDL: `docs/baseline/database/n8n.schema.sql`.
- Plan: `docs/baseline/backup-plan.md`.
- Bundle externo: `%USERPROFILE%\RHIA-Backups\rhia-postgres-20260820T195344Z`.
- 144 heartbeats RHIA incluidos en el backup.
- 10 workflows n8n, 6 activos.

## Riesgos

- Solo existe una copia local del bundle; falta una segunda ubicación independiente.
- El backup n8n contiene datos sensibles y debe permanecer cifrado fuera del repositorio.
- `rhia_admin` tiene privilegios de superuser.

## Decisiones

- No leer payloads, resultados, credenciales ni filas personales.
- Custodiar la passphrase mediante ACL privada y no imprimirla ni versionarla.
- Mantener 7 bundles válidos; diferir la poda automática y la segunda copia a `PH10-T004`.
- No modificar grants durante PH01.

## Siguiente tarea

Iniciar `PH01-T004`, ya desbloqueada por la finalización de `PH01-T002` y `PH01-T003`.
