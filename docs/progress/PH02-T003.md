# Registro de tarea PH02-T003

```text
TASK: PH02-T003
STATUS: DONE
COMMIT: No creado; publicación bajo control humano.
MIGRATIONS: Ninguna.
```

## Cambios

- Creado `@rhia/contracts` con schemas TypeScript/Zod estrictos y versionados.
- Definidos JobRequest/Result, ExecutionEvent, ToolCall/Result, SearchRequest/Response, ApprovalRequest/Decision, CallbackEnvelope y RhiaError.
- Agregado generador determinista de JSON Schema draft 2020-12.
- Documentados estados, callbacks, idempotencia, versionado, taxonomía de errores y mapeo desde campos n8n observados.

## Pruebas

- Typecheck strict: aprobado.
- Build y generación de schemas: aprobados.
- Contract tests: 7/7 aprobados.
- Auditoría npm de producción: 0 vulnerabilidades.
- Verificador del repositorio: 22 controles aprobados.

## Evidencia

- 11 schemas JSON válidos y 11 ejemplos JSON sintéticos válidos.
- Todos los ejemplos se validan automáticamente contra su schema runtime.
- Se rechazan versión ausente/futura, campos desconocidos, nombres de secreto, correlación inconsistente, códigos libres y estados resultado/error inválidos.
- Búsqueda sin resultados puede ser saludable; salud de proveedor se modela separadamente.
- Cero nombres de campos sensibles en src, ejemplos y schemas generados.

## Riesgos

- Los refinements cruzados de Zod son runtime y no todos se expresan de forma completa en JSON Schema; consumidores externos deben usar contract tests además del documento generado.
- Los workflows actuales todavía usan nombres legacy; sus adapters se implementarán sin trasladar payloads libres al Core.
- Compatibilidad real entre versiones futuras requiere dual-read y fixtures, aún no necesarios para v1.0.

## Decisiones

- Rechazar propiedades desconocidas en todos los límites.
- Toda modificación de forma crea una versión explícita; no hay cambios silenciosos a v1.0.
- Callbacks se deduplican por callbackId y conservan el mismo ID durante reintentos.
- Retryable es explícito y nunca se infiere desde texto.

## Siguiente tarea

Ejecutar `PH02-T004`: clasificar configuración no técnica y definir settings versionados con validación y defaults seguros.
