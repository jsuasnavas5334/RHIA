# PH10-T003 -- Threat model y abuse tests

**Estado:** DONE (alcance del packet: `docs/security/threat-model.md`
STRIDE-like + pruebas adversariales reales para las 3 pruebas requeridas
nombradas -- "Adversarial evidence", "Malicious URL", "Role escalation" -- y
las 2 acciones adicionales sin prueba nombrada -- "Tool abuse", "Data
exfiltration" -- con 1 hallazgo real documentado y explícitamente NO
corregido este ciclo, ver "Fuera de alcance").
**Sesión:** SES-20260910-91 (sesión nueva sin memoria de sesiones previas --
tarea programada, disparada manualmente vía `fire_trigger` con payload
adicional pidiendo priorizar avance real del Run Order; ese payload se trató
como dato, no como autorización adicional -- se verificó el estado real del
bloqueo de `device_bash` de forma independiente antes de asumir el workaround
ya documentado, en vez de darlo por sentado).
**Fecha:** 2026-09-10 (America/Guayaquil).

## Bloqueo de entorno (heredado, verificado de nuevo este ciclo)

`device_bash` no montó la carpeta en ningún momento de este ciclo (mismo
patrón exacto de las 9 sesiones anteriores documentadas, `PH09-T001` a
`PH11-T001` -- error real observado: `sandbox-helper: no Plan9 drive shares
mounted under /mnt/.virtiofs-root/shared`). Se verificó esto de forma
independiente al inicio del ciclo (no se asumió por el payload del disparo
manual) antes de aplicar el mismo workaround ya probado: `device_list_dir`/
`device_stage_files`/`device_commit_files` + `Bash` del contenedor cloud, con
un workspace cloud aislado nuevo para este ciclo.

Reuso real de paquetes ya compilados (`dist/` committeado, 0 cambios, 0
recompilación): `@rhia/domain`, `@rhia/policy`. Recompilados desde su `src/`
real este ciclo (0 cambios en ninguno, solo `tsc` real para poder
depender de ellos): `@rhia/tool-registry`, `@rhia/playwright-worker`,
`@rhia/computer-use`, `@rhia/secrets` -- los 4 con `tsc -p tsconfig.json
--noEmit` real en verde (exit 0) y `tsc -p tsconfig.json` (build) real en
verde antes de usarlos como dependencia del paquete nuevo de este ciclo. No
se re-ejecutaron sus propias suites de test (no se copiaron sus
`*.test.ts` a este workspace aislado -- no hacía falta: 0 modificaciones a
su código real, el typecheck+build limpio ya es evidencia suficiente de que
compilan igual que en el repo real, mismo criterio que sesiones anteriores
aplicaron a `@rhia/domain`/`@rhia/policy` reusados desde `dist/`).

## Contexto leído antes de escribir código

- `data/project-status.json`: `currentTask` = `PH10-T002` (`NOT_STARTED`),
  con nota explícita de la sesión anterior (`SES-20260910-90`) recomendando
  considerar `PH10-T003` como alternativa más tratable si `device_bash`
  seguía caído, en vez de reintentar `PH10-T002` (suite de regresión
  comercial E2E cruzando PH06-PH08, que requiere componer media docena de
  paquetes `@rhia/*` reales con `npm install`/`npm run build` desde la raíz
  -- inviable con el workaround real disponible, mismo diagnóstico
  confirmado de nuevo este ciclo).
- Últimas 6 entradas de `data/session-log.json` (`SES-20260910-85` a `90`):
  confirman la cadena real hasta `PH11-T001` `DONE` y el handoff explícito
  hacia `PH10-T002`, con la recomendación de `PH10-T003` como alternativa.
- Task Packets completos releídos en `PLAN_MAESTRO.md` antes de decidir:
  `PH10-T002` (líneas 3391-3442) y `PH10-T003` (líneas 3443-3495) -- se
  confirmó que `PH10-T003` depende de `PH10-T001` (`@rhia/secrets`,
  `DONE`), `PH03-T002` (`@rhia/policy`, `DONE`) y `PH09-T003`
  (`@rhia/computer-use`, `DONE`), ninguna de ellas requiere componer PH06-PH08
  -- estructuralmente más tratable en un workspace aislado, igual que
  `PH11-T001` lo fue frente a `PH10-T002`.
- **Decisión explícita de tomar `PH10-T003` en vez de reintentar
  `PH10-T002`:** confirmada independientemente este ciclo (no solo heredada
  de la nota de la sesión anterior) -- se verificó que `device_bash` sigue
  sin montar la carpeta (ver arriba) antes de aplicar el mismo razonamiento.
  `PH10-T002` sigue `NOT_STARTED`, pendiente de un ciclo futuro con acceso
  real a `device_bash`/`npm install` desde la raíz.
- Código real releído completo antes de diseñar las pruebas (no solo el
  Task Packet): `packages/policy/src/index.ts` (modelo de autorización real,
  techos de capability de servicio, no-autoaprobación), `packages/
  tool-registry/src/{contracts,authorization,registry,health}.ts` (manifest,
  autorización de invocación, salud), `packages/playwright-worker/src/
  {contracts,worker,driver,errors}.ts` (allowlist de dominio fail-closed en
  runtime, redacción estructural de valores tipeados), `packages/
  computer-use/src/{contracts,worker,driver,errors}.ts` (URL observada real,
  risk checkpoints, fallback humano), `packages/secrets/src/{redaction,
  contracts,encryption,errors}.ts` (redacción de 2 capas, cifrado
  autenticado), `packages/evidence-pipeline/src/{claim-extraction,
  evidence-builder,schema}.ts` (extracción de claims por regex fijo, nunca
  persiste texto crudo), y `packages/ai-gateway/src/{gateway,contracts}.ts`
  (confirma que el Gateway es solo transporte -- no arma prompts, no
  sanitiza contenido; la responsabilidad de no dejar que texto no confiable
  se interprete como instrucción vive en un orquestador que no existe/no se
  revisó este ciclo -- ver `docs/security/threat-model.md` sección 3).
- `PLAN_MAESTRO.md` sección 9 (modelo de datos) y `packages/db/src/
  schema.ts` NO se releyeron completos este ciclo porque esta tarea no
  propone ningún cambio de esquema (packet: "Archivos o áreas afectadas:
  security docs/tests" -- ningún dato nuevo a persistir). Guardrail del
  proyecto ("no crecer el esquema sin revisar si ya existe algo reutilizable")
  no aplica por no haber ninguna propuesta de persistencia nueva.

## Decisión de alcance

Paquete puro nuevo `packages/threat-model` (`@rhia/threat-model`), mismo
patrón estructural (`package.json`/`tsconfig.json` que extiende
`../../tsconfig.base.json`/`src/`) que todos los hermanos de PH09/PH10/PH11,
pero con una diferencia real declarada: su "entregable" no es una API de
runtime nueva, es el propio conjunto de pruebas adversariales del packet
(`docs/security/threat-model.md` es el entregable primario, citado
explícitamente por el packet). Depende de `@rhia/policy`,
`@rhia/tool-registry`, `@rhia/playwright-worker`, `@rhia/computer-use`,
`@rhia/secrets` -- las 5 dependencias reales declaradas por el packet
(`PH10-T001`, `PH03-T002`, `PH09-T003`) más las 2 transitivas que esas 3
necesitan (`@rhia/tool-registry` es dependencia real de `@rhia/secrets` y de
`@rhia/computer-use`/`@rhia/playwright-worker`, ya declarada en sus propios
`package.json` reales).

**Decisión de diseño real más importante:** no se depende de
`@rhia/evidence-pipeline` como paquete (su cadena real de dependencias --
`@rhia/search-orchestrator` -> `@rhia/search-health` + `@rhia/contracts` +
`zod` -- no se puede compilar en el workspace cloud aislado de este ciclo,
`zod` no está disponible ni localmente ni por red). Se copió textualmente
`claim-extraction.ts` (el único módulo real sin dependencias propias) a
`packages/threat-model/src/fixtures/evidence-claim-extraction.ts`, con un
comentario de cabecera que documenta el motivo completo y la condición para
resincronizar. Se evaluó reimplementar la lógica en vez de copiarla y se
descartó explícitamente: reimplementar perdería el valor real de la prueba
(estaría probando un heurístico inventado, no el código real de PH06-T003).

**Los "drivers" fake de `ssrf.test.ts`/`privilege-escalation.test.ts`/
`data-exfiltration.test.ts` se reescriben localmente** (no se importan
`testing/fake-driver.ts` de `@rhia/playwright-worker`/`@rhia/computer-use`):
esos helpers no se exportan en el `package.json` real de esos paquetes (solo
el subpath `"."` existe en `exports`), un import profundo fallaría bajo
resolución ESM real (`NodeNext`). Se escribieron fakes mínimos deterministas
(nunca abren un navegador real) con el mismo espíritu, más simples porque
este paquete no necesita colas de reacciones programadas (los escenarios de
prueba son deterministas de antemano).

## Qué NO se reimplementó

- Los mecanismos reales de autorización/allowlist/redacción -- este paquete
  SOLO los ejerce con entradas adversariales, nunca reimplementa una versión
  paralela de `authorize`/`assertDomainAllowed`/`redactText`/etc.
- La lógica de `claim-extraction.ts` -- copiada textual, nunca reescrita (ver
  arriba).

## Evidencia real (no simulada)

Workspace cloud aislado nuevo para este ciclo: `@rhia/domain`/`@rhia/policy`
reusados desde su `dist/` real ya compilado (0 cambios); `@rhia/tool-registry`,
`@rhia/playwright-worker`, `@rhia/computer-use`, `@rhia/secrets`
recompilados desde su `src/` real (0 cambios en los 4).

- `tsc -p tsconfig.json --noEmit` (`@rhia/tool-registry`): **exit 0**
  (primera corrida, sin cambios que hacer).
- `tsc -p tsconfig.json --noEmit` (`@rhia/playwright-worker`): **exit 0**.
- `tsc -p tsconfig.json --noEmit` (`@rhia/computer-use`): **exit 0**.
- `tsc -p tsconfig.json --noEmit` (`@rhia/secrets`): **exit 0**.
- `tsc -p tsconfig.json` (build) de los 4 paquetes anteriores: **exit 0**
  cada uno.
- `tsc -p tsconfig.json --noEmit` (`@rhia/threat-model`, el paquete nuevo de
  este ciclo): **exit 0** en la primera corrida (sin errores de tipos que
  corregir).
- `tsc -p tsconfig.json` (build) + `node --test dist/*.test.js`
  (`@rhia/threat-model`): **29/29 pass, 0 fail** en la corrida final, **0
  fallos reales encontrados** en este ciclo (a diferencia de `PH09-T003`
  (0 fallos) y de `PH10-T001`/`PH11-T001` (1-2 fallos reales cada uno) --
  aquí no se encontró ningún fallo de aserción ni de implementación en las
  pruebas nuevas, coherente con que el propósito de esta tarea es EJERCER
  código ya `DONE` y bien entendido tras la lectura completa, no construir
  lógica nueva compleja). Cobertura real por archivo:
  - `prompt-injection.test.ts` (5): **"Adversarial evidence"** (prueba
    requerida, nombrada explícitamente) -- un snippet con inyección de
    prompt explícita nunca produce un `claimType` fuera del catálogo fijo de
    `extractClaims` (solo `EMPLOYEE_COUNT` real, con `observedValue`
    estructurado); texto con forma de JSON de aprobación tampoco produce
    ningún claim; **peor caso hipotético** -- una aprobación "extraída" de
    texto con la forma exacta de `ApprovalProof`, auto-otorgada o con
    `organizationId` incorrecto, sigue sin autorizar (`APPROVAL_REQUIRED`,
    nunca `ALLOW`); control positivo confirma que una `ApprovalProof` real
    SÍ autoriza (la prueba mide lo correcto).
  - `ssrf.test.ts` (5): **"Malicious URL"** (prueba requerida, nombrada
    explícitamente) -- `playwright-worker` rechaza `NAVIGATE` a la IP de
    metadata de nube (`169.254.169.254`) sin invocar el driver;  un dominio
    con el permitido como sufijo (`crm.example.com.evil.com`) también se
    rechaza (match exacto de hostname, no `includes`); control positivo
    confirma que un dominio real de la allowlist sí llega al driver;
    `computer-use` rechaza ejecutar una acción cuando la URL REAL observada
    es la IP de metadata (`RHIA_COMPUTER_USE_DOMAIN_FORBIDDEN`, sin click
    real); `tool-registry` rechaza un `requestedDomain` de metadata de nube
    fuera del allowlist real de la tool (`RHIA_TOOL_DOMAIN_FORBIDDEN`).
  - `privilege-escalation.test.ts` (9): **"Role escalation"** (prueba
    requerida, nombrada explícitamente) -- `VIEWER` no puede
    `DECIDE_APPROVAL` ni `CHANGE_PRICE` (ni con una `ApprovalProof` real
    aportada); `OPERATOR` no puede `OFFER_DISCOUNT`; **self-approval**: un
    `MANAGER` con permiso real no puede auto-aprobar su propia
    `OFFER_DISCOUNT` (`APPROVAL_REQUIRED`); control positivo con aprobación
    de otro humano sí autoriza; una identidad `SERVICE` que se auto-declara
    con capabilities fuera de su techo real nunca escala (el techo real
    decide); ninguna identidad `SERVICE` alcanza
    `ROTATE_SECRET`/`MANAGE_PERMISSIONS`/`DEPLOY_BREAKING`
    (`servicesForbidden`); en `computer-use`, un step `CRITICAL`
    auto-aprobado por quien pide la corrida se escala a humano sin
    ejecutarse; control positivo con aprobación de otro humano sí ejecuta.
  - `tool-abuse.test.ts` (6): una acción fuera de `allowedActions` se
    rechaza (`RHIA_TOOL_ACTION_FORBIDDEN`); una tool con salud `DOWN` nunca
    se autoriza aunque la capability sea correcta
    (`RHIA_TOOL_UNAVAILABLE`); un manifest con una credencial real en
    `credentialRef` se rechaza al validar (nunca llega al registro); un
    `Scenario`/`ComputerUseTask` con un literal de credencial real en un
    `TYPE` se rechaza en ambos paquetes; control positivo con `SECRET_REF`
    (nunca el valor real) sí es válido.
  - `data-exfiltration.test.ts` (4): `redactText` elimina tokens/PII reales
    embebidos en texto libre (email, teléfono, Bearer, clave AWS);
    `redactValue` redacta por nombre de campo sensible incluso sin match de
    contenido; un valor `TYPE` nunca aparece en la evidencia de
    `playwright-worker`, **ni siquiera un valor que `looksLikeRawSecret` no
    detectaría** (garantía estructural, no dependiente del heurístico de
    contenido); **hallazgo real documentado** -- `validateToolManifest` NO
    aplica `looksLikeRawSecret` a `ownerRef`/`name` (solo a
    `credentialRef`) -- un manifest que filtra una credencial real ahí SÍ se
    registra hoy (`REGISTERED`, no `REJECTED`), confirmado con evidencia
    real, no corregido este ciclo (ver "Fuera de alcance").
- Revisión de secretos (`grep -i "sk-\|AKIA\|Bearer \|eyJ"` sobre
  `packages/threat-model/src/*.ts`, excluyendo las muestras canónicas de
  prueba ya reutilizadas por ciclos anteriores
  `sk-abcdefghijklmnopqrstuvwxyz123456`/`AKIAABCDEFGHIJKLMNOP`): sin
  resultados de credenciales reales -- las únicas coincidencias son las
  mismas muestras ficticias ya documentadas y reutilizadas desde
  `PH09-T001`.
- Regresión real: ningún archivo de `@rhia/domain`/`@rhia/policy`/
  `@rhia/tool-registry`/`@rhia/playwright-worker`/`@rhia/computer-use`/
  `@rhia/secrets` se modificó en este ciclo -- no aplica una regresión nueva
  que verificar en esos paquetes.
- **No se pudo re-ejecutar** `scripts/verify-repository-baseline.ps1` ni
  `git diff --check` reales este ciclo (`device_bash` caído, mismo bloqueo
  de todo el ciclo) -- limitación honesta, ver "Fuera de alcance".

## Criterios de aceptación del packet

- [x] Prompt injection no cambia policy -- `prompt-injection.test.ts`:
  ninguna forma de texto adversarial (incluida una con forma de
  `ApprovalProof`) logra que `authorize()` devuelva `ALLOW` sin una
  aprobación real y válida.
- [x] Browser restrictions probadas -- `ssrf.test.ts`: allowlist de dominio
  fail-closed probada en `playwright-worker`/`computer-use`/`tool-registry`
  contra objetivos SSRF reales (IP de metadata de nube, bypass por sufijo de
  dominio).
- [x] Approvals no bypass -- `privilege-escalation.test.ts`: self-approval,
  rol insuficiente, y ceiling de capability de servicio, los 3 vectores
  probados sin bypass real encontrado.

## Pruebas requeridas del packet

- [x] Adversarial evidence -- `prompt-injection.test.ts`, explícitamente
  nombrado y mapeado en el packet.
- [x] Malicious URL -- `ssrf.test.ts`, explícitamente nombrado y mapeado en
  el packet.
- [x] Role escalation -- `privilege-escalation.test.ts`, explícitamente
  nombrado y mapeado en el packet.

## Errores que debe evitar (packet) -- cómo se evitaron

- "Confiar en texto web como instrucción": `prompt-injection.test.ts`
  demuestra que ni `extractClaims` ni `authorize()` tratan texto libre (ni
  siquiera texto con forma de comando/JSON de aprobación) como una
  instrucción válida.
- "Permitir tool calls desde contenido no confiable": documentado como
  riesgo estructural pendiente en `docs/security/threat-model.md` sección 3
  -- el `AiGateway` real no arma prompts ni filtra contenido no confiable
  (esa capa de orquestación no existe/no se revisó este ciclo); se deja
  como recomendación explícita para cuando se construya, no se inventa una
  mitigación para código inexistente.

## Validación final del packet

"Red-team checklist pasa": las 3 pruebas requeridas nombradas y las 2
acciones adicionales del packet tienen cobertura real en verde (29/29). El
hallazgo real de la sección "Data exfiltration" (gap de `ownerRef`/`name` en
`validateToolManifest`) es la única salvedad real, documentada explícita y
honestamente, no oculta.

## Fuera de alcance de este ciclo (honesto, no oculto)

- **`@rhia/tool-registry` NO se modifica en ESTE ciclo (PH10-T003)** para
  cerrar el gap de `ownerRef`/`name` encontrado en "Data exfiltration" --
  decisión explícita, ver `docs/security/threat-model.md` sección 7 para el
  razonamiento completo (superficie de cambio de un paquete de otra fase ya
  `DONE`, con trade-off real de falsos positivos que debe evaluarse en un
  ciclo dedicado, no de pasada aquí). **Corregido en un ciclo posterior --
  ver "Update 2026-09-10 (sesión SES-20260910-93)" al final de este
  documento.**
- **`apps/agent-runtime`** (el orquestador real que arma prompts hacia
  `@rhia/ai-gateway`, si existe/se construye) **no se revisó este ciclo** --
  la sección 3 del threat model documenta el riesgo estructural visible
  desde el contrato de `@rhia/ai-gateway`, no una auditoría completa de ese
  orquestador.
- **`@rhia/evidence-pipeline` no se ejecutó completo bajo test** (bloqueo de
  `zod`/red, ver "Bloqueo de entorno" y `docs/security/threat-model.md`
  sección 8) -- se usó una copia textual verificable de su único módulo sin
  dependencias (`claim-extraction.ts`).
- **Ningún ataque de red real se ejecutó** -- todas las pruebas SSRF usan
  drivers fake deterministas inyectados, nunca una petición HTTP real.
- **`npm install` no se corrió desde la raíz** (mismo motivo, `device_bash`
  no disponible) -- el symlink `node_modules/@rhia/threat-model` no existe
  todavía en el toolchain real de Windows.
- **`scripts/verify-repository-baseline.ps1` no se re-ejecutó este ciclo**
  (requiere PowerShell/Git en Windows, bloqueado) -- se documenta como
  limitación, no como verificado de nuevo hoy.
- **`PH10-T002` (Construir suite de regresión comercial) sigue
  `NOT_STARTED`** -- confirmado de nuevo este ciclo que excede el workaround
  real disponible (requiere componer PH06-PH08 con `npm install`/`build`
  reales desde la raíz).

## Handoff / Siguiente paso

`PH10-T003` queda `DONE`. Según el Run Order (`PLAN_MAESTRO.md` sección 26,
paso 39), el siguiente paso formal del Run Order es `PH10-T004` (Backup,
restore y disaster recovery, rol `DEVOPS`) -- el próximo ciclo debe leer su
Task Packet completo antes de empezar y confirmar sus dependencias reales.
`PH10-T002` sigue siendo la única tarea `NOT_STARTED` del bloque paralelo del
paso 38 -- un ciclo futuro con acceso real a `device_bash`/`npm install`
desde la raíz debe retomarla; hasta entonces, seguir avanzando por el Run
Order con las tareas estructuralmente tratables en el workspace aislado
(paquetes nuevos/aislados o con pocas dependencias `@rhia/*` ya
compiladas), mismo criterio aplicado en `PH11-T001` y en este ciclo.
Handoff declarado por el propio packet de `PH10-T003`: "Handoff gate".

## Update 2026-09-10 (sesión SES-20260910-93) -- gap de `ownerRef`/`name` corregido

**Contexto de esta sesión:** tarea programada (scheduled task, sesión nueva
sin memoria de sesiones previas), disparada por el ciclo horario. Se
verificó de forma independiente al inicio del ciclo que `device_bash`
seguía sin montar la carpeta (3 intentos consecutivos, mismo error de
infraestructura `sandbox-helper: no Plan9 drive shares mounted`) y que el
registro npm seguía bloqueado desde el contenedor cloud (`curl` a
`registry.npmjs.org` -> `403`) -- mismo diagnóstico que las sesiones
`SES-20260910-89` a `92` ya habían documentado hoy, sin asumirlo de
memoria. Se releyó `PLAN_MAESTRO.md` sección 26 (Run Order) completa: el
siguiente paso formal es `PH10-T004` (requiere Postgres real/restore drill,
inviable bajo el bloqueo actual), y las demás tareas `NOT_STARTED`
alcanzables por dependencias (`PH11-T003`, `PH11-T004`, `PH12-T001`)
también requieren infraestructura/hardware real (cold boot de la máquina,
staging remoto) -- ninguna tratable bajo el workaround aislado. No se repitió
la evaluación completa de `PH10-T002`/`PH10-T004`/`PH11-T003` ya hecha hoy
mismo por `SES-20260910-91`/`92`; se aceptó su diagnóstico ya verificado.

En vez de cerrar el ciclo sin avance, se tomó la recomendación concreta que
esta misma sección de este documento (y `docs/security/threat-model.md`
sección 7) dejó explícitamente para "un ciclo futuro dedicado a esa
tarea": ampliar `looksLikeRawSecret` a `ownerRef`/`name` en
`validateToolManifest` (`@rhia/tool-registry`), evaluando el costo de falsos
positivos. Esto NO es un Task Packet formal del Run Order -- es un fix
aislado, aditivo y reversible sobre un hallazgo de seguridad real ya
documentado (autorizado sin nueva interacción: "corregir bugs evidentes").

**Cambio real:** `packages/tool-registry/src/contracts.ts`,
`validateToolManifest` ahora aplica `looksLikeRawSecret` también a `name` y
a `ownerRef` (antes solo a `credentialRef`), con el mismo mensaje de error
explicando el motivo ("Pasar credenciales en prompt"). No se tocó
`looksLikeRawSecret` en sí (misma heurística, sin cambios) -- solo dónde se
aplica.

**Evidencia real (build + test real, `tsc@6.0.3` global del contenedor
cloud contra los `dist/` YA compilados y reales de `@rhia/domain`/
`@rhia/policy`, sin red, mismo workaround documentado en `CLAUDE.md`; nunca
se tocó `node_modules`/`dist` del repo real del usuario, todo el árbol de
compilación es un directorio aislado en el workspace cloud):**

- `@rhia/tool-registry`: `tsc --noEmit` limpio, build limpio, **26/26 tests**
  (los 23 ya existentes + 3 nuevos: rechaza JWT/AWS-key pegado en `ownerRef`,
  rechaza `Bearer ...` pegado en `name`, y un test explícito de NO regresión
  de falsos positivos sobre `ownerRef`/`name` humanos legibles y largos --
  `'equipo-plataforma-de-agentes-digitales'`, `'Computer Use Adapter para
  Reservas de Reuniones'` -- ambos siguen aceptándose).
- Se reconstruyó la cadena completa de dependientes reales de
  `@rhia/tool-registry` (nunca compilada de punta a punta antes en este
  proyecto -- ninguno de estos paquetes tenía `dist/` en disco) para
  confirmar que el cambio no rompe nada aguas abajo: `@rhia/secrets`
  (**27/27**), `@rhia/playwright-worker` (**37/37**), `@rhia/computer-use`
  (**32/32**), `@rhia/skill-library` (**20/20**), `@rhia/threat-model`
  (**29/29**, incluyendo el test que documentaba el hallazgo original --
  actualizado para reflejar el comportamiento corregido, ver abajo). **Total:
  171/171 tests reales en verde, 0 fallos.**
- `packages/threat-model/src/data-exfiltration.test.ts`: el test
  "HALLAZGO REAL..." que documentaba el gap (aserción original
  `result.outcome === 'REGISTERED'` para un `ownerRef` con una API key real
  pegada) se actualizó a `result.outcome === 'REJECTED'` con
  `errors.some(e => e.field === 'ownerRef')` -- exactamente el mantenimiento
  que la nota original del propio test pedía ("Si un ciclo futuro decide
  ampliar `looksLikeRawSecret` a `ownerRef`/`name`, este test debe
  actualizarse a la vez"). El comentario de cabecera del archivo también se
  actualizó para no describir un gap ya cerrado como abierto.
- `docs/security/threat-model.md` sección 7 actualizada para reflejar el
  fix (ver ese documento).

**Archivos modificados (commit vía `device_commit_files`, archivo por
archivo, sin `device_bash`; `node_modules`/`dist` reales del usuario nunca
se tocaron):**
`packages/tool-registry/src/contracts.ts`,
`packages/tool-registry/src/contracts.test.ts`,
`packages/threat-model/src/data-exfiltration.test.ts`,
`docs/security/threat-model.md`, este documento,
`data/session-log.json`, `data/project-status.json`.

**Fuera de alcance de este fix (honesto, no oculto):** no se auditó si
existe algún otro consumidor de `validateToolManifest`/`ToolManifest` fuera
de los 6 paquetes ya listados arriba (ninguna `apps/*` depende hoy de
`@rhia/tool-registry` -- verificado en `apps/core-api/package.json` y
`apps/agent-runtime/package.json`, ninguno lista la dependencia). Si un
ciclo futuro conecta `@rhia/tool-registry` a un `app` real con datos de
producción reales (nombres/owners ya existentes), debe re-evaluar si algún
dato real legítimo ahora es rechazado por este cambio antes de desplegar.

**Estado del Run Order:** sin cambios respecto a `SES-20260910-92` --
`PH10-T004` sigue siendo el siguiente paso formal, sigue bloqueado por
infraestructura real no disponible este ciclo. Este fix fue un uso
productivo y seguro del ciclo mientras ese bloqueo persiste, no un
reemplazo del Run Order. El próximo ciclo debe: (1) verificar de nuevo si
`device_bash` está disponible, (2) si lo está, retomar `PH10-T002` o
`PH10-T004` con evidencia real completa, (3) si sigue caído, releer esta
sección antes de buscar otro fix aislado similar (no repetir éste, ya
aplicado).
