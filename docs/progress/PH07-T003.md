# PH07-T003 -- Validar puntos de contacto

**Estado:** DONE (alcance "Contact Validation v1" del packet -- backend real wireado en `apps/core-api`, sin provider externo real ni vista de frontend; ver "Fuera de alcance de este ciclo").
**Sesion:** SES-20260906-70 (Cowork, ciclo programado, carpeta `Software RHIA` conectada via `device_bash`, misma conversacion que `PH07-T002`).
**Fecha:** 2026-09-06 America/Guayaquil (2026-09-07 madrugada UTC).

## Contexto leido antes de escribir codigo

- `docs/progress/PH07-T002.md` completo (recien cerrado en esta misma sesion): confirma que `PH07-T002` es dependencia satisfecha y deja documentado que `product`/`price_book` (PH03-T001) SI existen en el repo pero sin uso -- leccion aplicada aqui tambien (ver mas abajo).
- Task Packet completo de `PH07-T003` en `PLAN_MAESTRO.md` (seccion 24, linea ~2677).
- `packages/db/src/schema.ts`: tabla `contact_point` (PH03-T001) -- YA tiene `value_encrypted` (bytea), `value_hash` (char 64), `validation_status`, `source_id` (FK a `evidence_source`), `last_validated_at`. `grep` confirmo que **ningun codigo en `apps/`/`packages/*/src` la usaba todavia** (mismo patron que `company_entity`/`company_location` antes de `PH07-T001`, y que `product`/`price_book` que `PH07-T002` recien corrigio) -- se reutiliza tal cual, no se crece el esquema.
- `apps/core-api/src/company-service.ts`, `ports.ts`, `record-services.ts`, `memory-adapters.ts`, `postgres-adapters.ts`, `api.ts`, `runtime.ts`, `core-api.test.ts`: patrones existentes de `ContactService`/`CompanyGroupService` (idempotencyKey + fingerprint + `unitOfWork.execute` + `audit.append`) replicados exactamente para el servicio nuevo.
- `grep -rn "encrypt\|cipher"` sobre `apps/`/`packages/*/src`: **ninguna utilidad de cifrado existia en el repo** -- se construyo con `node:crypto` (stdlib, AES-256-GCM), sin agregar ninguna dependencia nueva.

## Objetivo del packet

Estimar vigencia y calidad de email/WhatsApp/otros puntos de contacto: normalizar, dedup por hash, validar formato, usar validators/provider cuando se configure, marcar confidence/freshness.

## Que se construyo

Backend real en `apps/core-api` (rol recomendado del packet: `BACKEND`), siguiendo el mismo patron ya establecido por `ContactService`/`CompanyGroupService`.

- `contracts.ts`: `ContactPointTypeSchema` (`EMAIL | PHONE | WHATSAPP`), `ContactPointValidationStatusSchema` (`UNVERIFIED | INVALID | VERIFIED`), `ContactPointSchema` (nunca expone el valor real, solo `valueMasked` + `valueHash`), `CreateContactPointSchema` (unico lugar que recibe `rawValue`, nunca se devuelve). `TimelineEventSchema` gano `CONTACT_POINT_CREATED`/`CONTACT_POINT` (accion/resourceType) para que el tipo del audit trail sea consistente -- en la practica estos eventos nunca aparecen en un timeline de Company 360 (`CompanyGroupService.getById` filtra por `resourceId` de company/contacts/opportunities, nunca de contact points), documentado como decision deliberada, no un descuido.
- `ports.ts`: `ContactPointRecord` (interno, incluye `valueEncrypted: Buffer`), `ContactPointRepository` (con `findByHash` -- la base del dedup), `EncryptionKeyProvider`; `CoreDependencies` gana `contactPoints`/`encryptionKeys`; `IdempotentResource` gana la variante `CONTACT_POINT`.
- `contact-point-crypto.ts` (nuevo): `encryptContactPointValue`/`decryptContactPointValue` (AES-256-GCM, `node:crypto`, IV+authTag+ciphertext empacados en un solo Buffer). `EnvEncryptionKeyProvider` lee `RHIA_CONTACT_POINT_ENCRYPTION_KEY` (64 hex = 32 bytes) y **falla ruidosamente** si falta o tiene formato invalido -- nunca una clave por defecto en el codigo (regla fija del proyecto: "secretos nuevos solo con autorizacion explicita y siempre fuera del repositorio"). `StaticEncryptionKeyProvider` (clave aleatoria generada en runtime, nunca un literal) para tests/desarrollo.
- `contact-point-service.ts` (nuevo): funciones puras `normalizeContactPointValue` (accion 1), `validateContactPointFormat` (accion 3, regex email/E.164), `hashContactPointValue` (accion 2, sha256 del valor normalizado), `maskContactPointValue` (nunca expone el valor completo), `isValidationStale`/`effectiveValidationStatus` (degrada `VERIFIED` -> `UNVERIFIED` en LECTURA cuando la validacion quedo vieja -- nunca un quinto estado persistido), `isSendableContactPoint` (excluye `INVALID`), `confidenceForStatus` (derivado, no persistido -- accion 5 sin crecer el esquema). Clase `ContactPointService` (`listByContact`/`create`) con el mismo patron idempotencyKey+fingerprint+`unitOfWork.execute`+`audit.append` que `ContactService`, mas dedup por hash (`findByHash`) ANTES de insertar, sin importar si la idempotencyKey es distinta.
- `memory-adapters.ts`/`postgres-adapters.ts`: `MemoryContactPointRepository`/metodos `createContactPoint`/`listContactPointsByContact`/`findContactPointByHash` en `PostgresCorePersistence` (INSERT/SELECT reales sobre `rhia.contact_point`, columna bytea mapeada directo a `Buffer`, sin conversion a texto en ningun punto).
- `api.ts`/`runtime.ts`: rutas `GET`/`POST /api/v1/contacts/:id/points`; `CoreApi` y `createPostgresCoreRuntime` wireados con `ContactPointService`.

## Evidencia real (no simulada)

- `tsc --noEmit` real (`/usr/local/lib/node_modules_global/bin/tsc`, nativo `7.0.2`) sobre `apps/core-api/tsconfig.json`: **exit 0** tras dos rondas de correccion (el `TimelineEventSchema` original no aceptaba la accion/resourceType nuevos usados por `company-service.ts`; el fixture de test no tenia `contactPoints`/`encryptionKeys`) -- ambas corregidas antes de continuar, no se relajo ningun tipo.
- Build real (mismo `tsc`) -> `apps/core-api/dist/*.js`: **exit 0**.
- `node --test apps/core-api/dist/*.test.js`: **61 pass / 0 fail / 2 skip** (los 2 `skip` son Postgres real, sin cambios -- 34 previos de `core-api.test.ts` + 5 nuevos de integracion + 22 nuevos de `contact-point-service.test.ts`, mas los ya existentes de `postgres-adapters.test.ts`). Un primer intento de la prueba de staleness fallo dos veces antes de pasar: (1) `Buffer.from('placeholder')` no era un payload AES-GCM real (IV/authTag invalidos) -- corregido cifrando con `encryptContactPointValue` real usando la misma clave del fixture; (2) los IDs sinteticos (`'point-verified-stale-001'`, `'contact-001'`) no eran UUIDs validos contra `ContactPointSchema` -- corregido con UUIDs v4 reales, mismo patron que el resto del archivo.
- Cobertura de las **Pruebas requeridas del packet**:
  - **Invalid email**: `validateContactPointFormat('EMAIL', 'no-es-un-email')` -> `false` (unit), mas integracion completa via `POST /api/v1/contacts/:id/points` -> `validationStatus: 'INVALID'`.
  - **Duplicate phone**: mismo telefono con dos formatos de entrada distintos (espacios vs guiones) y dos `idempotencyKey` DISTINTAS -> una sola fila en `contact_point`, un solo evento `CONTACT_POINT_CREATED` en el audit trail, la segunda respuesta HTTP es `200` (no `201`) con el mismo `id`.
  - **Stale validation**: un punto `VERIFIED` con `lastValidatedAt` de hace 400 dias (> `DEFAULT_VALIDATION_STALE_AFTER_DAYS = 180`) se lee como `UNVERIFIED` via `listByContact` -- mas 3 tests unitarios de `isValidationStale`/`effectiveValidationStatus` (stale, reciente, sin validar nunca).
- Cobertura de los **3 criterios de aceptacion**:
  - **"No envia a INVALID"**: `isSendableContactPoint({validationStatus:'INVALID'})` -> `false` (unit); `effectiveValidationStatus` nunca "recupera" un `INVALID` por antiguedad (test explicito).
  - **"Unknown no se presenta como verified"**: test de staleness arriba -- un `VERIFIED` viejo se presenta como `UNVERIFIED`, nunca sigue mostrandose como confirmado.
  - **"PII protegida"**: test de integracion dedicado -- `JSON.stringify(response.body)` NO contiene el email real; `valueMasked` nunca es igual al valor real; el `Buffer` cifrado persistido (`contactPoints.records[0].valueEncrypted`, decodificado como latin1 para buscar substrings) NO contiene el email ni el local-part en claro (AES-256-GCM produce ciphertext indistinguible de datos aleatorios). Ademas: `hashContactPointValue` nunca incluye el valor real como substring de su digest (test explicito); el audit trail (`afterHash`) se calcula sobre `{id, pointType, validationStatus}`, nunca sobre el valor ni su hash expuesto en claro.
- Permission test adicional (no exigido literalmente por el packet, pero mismo patron que el resto del modulo): `viewer` (sin `WRITE_OPERATIONS`) recibe `403` al intentar crear un contact point.
- `tsc --noEmit` de `apps/web` (regresion, no se toco): **exit 0**.
- `grep -rl " $"` / `grep -rlP '\t'` sobre `apps/core-api/src/*.ts` (archivo completo, no solo los nuevos): sin resultados.
- Revision manual de secretos (`grep -rniE "password|secret|api_key|apikey"`) sobre los archivos nuevos: sin resultados relevantes (las unicas coincidencias son el NOMBRE de la variable de entorno `RHIA_CONTACT_POINT_ENCRYPTION_KEY` y comentarios explicando la regla de "secretos fuera del repositorio", nunca un valor real).

## Criterios de aceptacion del packet

- [x] No envia a INVALID -- `isSendableContactPoint` excluye `INVALID` sin excepcion, verificado con test.
- [x] Unknown no se presenta como verified -- `effectiveValidationStatus` degrada `VERIFIED` stale a `UNVERIFIED` en lectura, verificado con test de integracion y 3 unitarios.
- [x] PII protegida -- valor real cifrado (AES-256-GCM) + solo hash/mascara expuestos, verificado con test que inspecciona tanto la respuesta HTTP como el registro persistido.

## Pruebas requeridas del packet

- [x] Invalid email.
- [x] Duplicate phone.
- [x] Stale validation.

## Errores que debia evitar (verificados)

- [x] Guardar secretos en logs -- ningun log/campo de audit contiene el valor real, su forma normalizada, ni siquiera transitoriamente en una variable que se loguee; el `afterHash` del audit trail se calcula sobre metadata no sensible, nunca sobre el valor. La clave de cifrado tampoco se loguea en ningun punto (`EnvEncryptionKeyProvider.getKey()` la lee y la devuelve, nunca la imprime).
- [x] Inventar emails sin marcar inferidos -- `normalizeContactPointValue` nunca completa ni corrige un valor (no agrega dominios, no infiere codigos de pais faltantes en telefonos sin `+`) -- documentado explicitamente en el comentario de la funcion.

## Fuera de alcance de este ciclo (no bloquean el cierre `DONE`, documentado explicitamente)

1. **Provider real de validacion** (accion 4, "usar validators/provider cuando se configure"): sin credenciales autorizadas para un servicio de verificacion de email/telefono real, `VERIFIED` queda **inalcanzable** desde `create()` en este ciclo -- el contrato y el status ya existen, listos para cuando se conecte un provider real (mismo patron que `PH05-T004` con proveedores de IA: infraestructura de medicion lista, sin credenciales reales).
2. **Aprovisionar `RHIA_CONTACT_POINT_ENCRYPTION_KEY` en produccion real**: es responsabilidad de operaciones humanas (regla fija del proyecto sobre secretos) -- `EnvEncryptionKeyProvider` esta listo pero sin esa variable configurada en el entorno real, cualquier intento de cifrar/descifrar en produccion fallaria explicitamente (comportamiento deseado: nunca una clave por defecto silenciosa).
3. **Vista de frontend** ("Validacion final" del packet: "Contact view muestra estado y fuente"): el packet declara rol recomendado `BACKEND`, no `FULLSTACK` -- este ciclo deja la API lista (`GET /api/v1/contacts/:id/points` expone `validationStatus` + `sourceId` + `lastValidatedAt`, todo lo que una vista necesitaria) pero **no se toco `apps/web`** para renderizarla. Queda para un ciclo de frontend dedicado o para cuando `PH07-T004`/outreach lo necesite de verdad.
4. **`sourceId`** (`FK` a `evidence_source`, PH03-T001/PH06-T003): el contrato lo acepta como opcional pero este ciclo no conecta ningun caller real que lo aporte (ni `@rhia/contact-discovery`, que no produce `EvidenceSource` persistido todavia -- ver "Fuera de alcance" de `PH07-T002`) -- queda `null` en la practica hasta que exista esa integracion.
5. Confidence como campo persistido: `confidenceForStatus` es una funcion derivada en lectura, no una columna nueva -- no se crecio el esquema para esto (misma regla fija ya aplicada en `PH07-T001`/`T002`).
6. `scripts/verify-repository-baseline.ps1` (PowerShell): no se pudo ejecutar desde este sandbox Linux -- mismo limite documentado en todos los ciclos anteriores.

## Siguiente tarea

`PH07-T003` esta `DONE`. Sigue `PH07-T004` -- Implementar Opportunity Scoring (dependencias `PH07-T001` DONE, `PH07-T003` DONE; rol recomendado `PRODUCT`). Leer su Task Packet completo en `PLAN_MAESTRO.md` (seccion 24, linea ~2731) antes de empezar, no asumir alcance -- en particular, revisar si `packages/db/src/schema.ts` ya tiene columnas reutilizables para scoring (`opportunity.score`/`score_version`/`opportunity_signal` YA EXISTEN de PH03-T001, confirmar con grep antes de crecer el esquema, mismo patron que los ultimos 3 ciclos).
